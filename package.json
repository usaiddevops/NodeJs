{
  "name": "playground",
  "version": "1.0.0",
  "description": "node test",
  "main": "app.js",
  "scripts": {
    "test": "jest",  // Replace this with your test command
    "start": "node app.js",
    "lint": "eslint .",
    "build": "echo \"No build script defined\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0" // Ensure Jest is installed if using Jest
  }
}
